# Leave empty to use Vite's local /api proxy. Set this for direct API access.
# Note: this value is inlined into the client bundle and is therefore public.
# Never put credentials or tokens in a VITE_* variable.
VITE_API_BASE_URL=
